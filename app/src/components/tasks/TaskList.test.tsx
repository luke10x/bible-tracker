describe('when one task is created', () => {
  const events = [{ id: 1, uuid: 'uuid1', title: 'hello1' }];
  it('should have one task', () => {
    // const expectedTasks = []
    // expect(eventsReducer(eventst)).toBe(expectedTasks)
  });
});
